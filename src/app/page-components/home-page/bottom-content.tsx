import { HomeData } from "@/lib/types";

type BottomContentProps = {
  homeData: HomeData;
};

export const BottomContent = ({ homeData }: BottomContentProps) => {
  return <div>This is the bottom content.</div>;
};
